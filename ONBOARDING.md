# ONBOARDING — Кадастровая карта Новых Регионов

> Шапка актуальна на 2026-07-19 (числа посчитаны с диска и подтверждены живой
> шапкой карты). Всё, что ниже разделителя — исторические слои, числа в них
> устарели, см. предупреждения.

**GitHub:** `github.com/x270880x/kadastr` (ветка `main`; репозиторий
инициализирован 2026-06-29 уже с готовыми 4 областями — в нём ровно один коммит).
Раньше проект был чисто локальным — это больше не так.

- **Локально:** `~/Documents/Дизайны/НР-КАДАСТРОВАЯ КАРТА/` (кириллица+пробел —
  в shell квотить). Переехал из `~/Documents/КАДАСТРОВАЯ КАРТА/` — старого пути
  больше нет.
- **Preview:** конфиг **`kadastr-nr`**, порт **8775**
  (`preview_start name:"kadastr-nr"`). ⚠️ Конфиг раньше назывался `zaporizhzhia`
  и указывал на несуществующий путь — сервер поднимался, но отдавал пустоту;
  починено и проверено запуском 2026-07-19. Бета — `/map-beta.html`.
- `BUILD_VERSION = 'v19'` в `index.html`. В `map-beta.html` его нет (в отличие
  от kherson, где версионируются обе страницы).

## Эталонные числа (2026-07-19, посчитано напрямую)

**ЧЕТЫРЕ области**, не две: **1901 совет**, **2 730 177 участков**,
**7 434 291 га**. Тайлы **54 010 `.pbf`, ~400 МБ**.
`councils-index.json` = 1901 запись, `councils/*.json` = 1901,
`parcels/*.geojson` = 1901 — согласовано.

| Область | Префикс КОАТУУ | Советов | Участков |
|---|---|---|---|
| Донецк | `14` | 533 | 804 066 |
| Запорожье | `23` | 630 | 679 935 |
| Луганск | `44` | 412 | 632 705 |
| Херсон | `65` | 326 | 613 471 |

`ONLY_REGIONS = ['Херсон','Запорожье','Донецк','Луганск']`; гербы
`gerb-kherson.svg` / `gerb-zap.svg` / `gerb-dnr.svg` / `gerb-lnr.svg`.
Общий герб — федеральный РФ, титул «Новые регионы».

**Слияние (репо kherson — ТОЛЬКО ЧТЕНИЕ, не трогать):** данные Херсона
скопированы байт-в-байт (все 326 `councils/65*.json` идентичны оригиналу).
Схема: `cp` parcels+councils → merge `councils-index.json` → `zap_embed.py` →
`RAION_BY_PREFIX` по 5-знач. ключу `code[0:5]` + `raionOf`→`slice(0,5)`
(3-знач. префиксы областей конфликтуют!) → `build_tiles.sh`. Имена Херсона
кураторские, остальных областей — геокод.

⚠️ **Пайплайн для ДНР/ЛНР не описан ниже.** Раздел «Как пересобрать данные с
нуля» покрывает только запорожский путь. Реальный 4-областной слой живёт в
`.wip/refetch/` (gitignored): `nr_scan.py`, `verify_don.py`, `build_oblasts.py`,
`zap_raion_combine.py`, каталоги `out_val_14`/`out_val_44`/`dedup_44`, логи
`don_build.log`/`luh_build.log`.

---
_Ниже — исторические слои: сначала документация объединения Херсон+Запорожье
(2026-06-24), затем исходная документация Запорожье-части._
_⚠️ **Числа в обоих слоях УСТАРЕЛИ** (956 советов / 1 293 406 участков и
630 / 679 935 соответственно) — актуальные см. в шапке выше. Пайплайн сбора и
описание структуры при этом остаются полезными._

## Слияние Херсон+Запорожье (2026-06-24, исторически)

Тогда: 956 советов (326 Херсон + 630 Запорожье), 1 293 406 участков,
4 287 982 га, тайлы 26 390/194 МБ. Ребрендинг: центр (47.0,34.55), границы
[[45.7,31.4],[48.3,37.5]]. Бета: `bounds` источника парцел
[31.4,45.7,37.5,48.3] + razmezh Херсона + gz-протокол отдаёт пустой тайл на 404.

---
_Исходная документация Запорожье-части (актуальна для пайплайна сбора)._

Дубликат [kherson] под Запорожскую область (КОАТУУ `23`).

## Где что
- ⚠️ УСТАРЕЛО, см. шапку: путь был `~/Documents/КАДАСТРОВАЯ КАРТА/`, git не было,
  конфиг превью назывался `zaporizhzhia`. Актуально: репо в
  `~/Documents/Дизайны/НР-КАДАСТРОВАЯ КАРТА/`, remote `x270880x/kadastr`,
  конфиг превью `kadastr-nr` (порт 8775).
  Полезное и по-прежнему верное: кириллица+пробел в пути — в shell квотить,
  в zsh `${=VAR}` для word-split.

## Эталонные числа
- **630 советов**, **679 935 участков** (после дедупа), **2 125 620 га**. До дедупа было 712 516; снято 32 581 дубль-стопка (порог 0.80).
- **Тайлы:** 13 677 `.pbf`, ~102 МБ, z6–14 (`build_tiles.sh`, tippecanoe).
- **20 районов** (+5 городов) — `RAION_BY_PREFIX` в index.html и map-beta.html по КОАТУУ-префиксу `code[2:5]`.

## Структура (как kherson)
- `index.html` — основная карта (Leaflet). Встроенный `<script id="data">` (сводки советов+stats+filters); регенерится `.wip/refetch/zap_embed.py`. `ONLY_REGION='Запорожье'`, центр обзора (47.30,35.75), `PAN_BOUNDS` [[46.20,34.10],[48.30,37.40]].
- `map-beta.html` — бета (MapLibre). razmezh-слой пуст (`razmezh.geojson` = пустой FC, у Запорожья нет РФ-межевания). `filters-beta.json` — категории (аренда/арендаторы пустые).
- `parcels/<code>.geojson` (630) — геометрия (только cadnum). `councils/<code>.json` (630) — атрибуты (`c,a,ot,ct,use`+стабы, `src:"zap2026"`). `councils-index.json` — `c,n,lat,lon,o{p,k,g,n},total,ha`.
- `gerb.svg` — герб Запорожской обл. (РФ-вариант, Wikimedia, минифицирован SVGO).
- `categories-ru.json`, `fonts/`, `build_tiles.sh` — из kherson.

## Источники и пайплайн (тулинг `.wip/refetch/`, gitignored)
Те же, что kherson [project_kherson_refetch]: геометрия — supabase tiles-proxy (z16, `tile_land_polygons`, cadnum); атрибуты — vkursi-proxy (форма собств.+площадь+категория+use); имена — Яндекс-геокодер. `kref.py` — либа, `Y_DOWN=False`, +5 м.
- `zap_build.py` — **валидированный билдер** (scan_fixed по детям z14-тайлов → ловит эксклавы; ремонт обрезки border-flag; финализация геометрии после округления: дедуп вершин + buffer(0); overlap-метрика). Пишет `out_val/`. Резюмируемый. Чекпойнт скана `out3/_seeds.json` (749 советов, 630 с ≥2 тайлами).
- `zap_dedup.py [thr]` — отдельный проход дедупа дублей-стопок (по умолч. 0.80) → `dedup/`.
- `zap_assemble.py` — `dedup/` → `parcels/`+`councils/`+`councils-index.json`.
- `zap_embed.py` — встроенный data-скрипт в index.html.
- `zap_names.py` — имена советов (Яндекс, центр жилого кластера). `_names.json`.
- `zap_raions.py` — геокод районов (но финальный RAION_BY_PREFIX задан вручную по КОАТУУ-схеме, сверен с геокодом; `_raions.json`).
- `zap_audit.py`, `verify_*.py` — QA.

## Качество (проверено адверсариальной верификацией)
Участки целые (IoU vs источник 0.999, обрезки нет), площадь точна (99% в 3%), геометрия валидна (0 невалидных после buffer(0)), потерь нет (метод-независимый скан: 0 пропаж), вершины почищены. **Налегания = реальные дубли реестра** (пере-межевание, вложенные права; 30/30 подтверждены в источнике) — порог дедупа **0.80**, не понижать.
- ⚠️ Известное: **30 участков CLIPPED_UNRESOLVED** + **20 OVER** (0.007%) — заявленная площадь vs геометрия тайлов расходятся (дефект источника), оставлены с геометрией, помечены в qa. **1685 NO_AREA** — vkursi без записи, площадь = геометрическая.
- ⚠️ **Имена 630 советов авто-геокодированы Яндексом** — править только с подтверждением [feedback_confirm_before_names_polygons].

## Как пересобрать данные с нуля
`cd .wip/refetch` → `python3 zap_build.py` (часы, кэш тайлов переиспользуется) → `python3 zap_dedup.py 0.80` → `python3 zap_assemble.py` → `python3 zap_names.py` → `python3 zap_embed.py` → `cd .. && ./build_tiles.sh`.
